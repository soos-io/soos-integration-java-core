package io.soos.integration;

public class Enums {

    public enum LogLevel {
        DEBUG, INFO, WARN, FAIL, ERROR
    }

    public enum OnFailure {
        FAIL_THE_BUILD, CONTINUE_ON_FAILURE
    }
    
}
